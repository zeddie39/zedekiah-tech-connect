import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ztech.electronics',
  appName: 'Ztech Electronics Limited',
  webDir: 'dist'
};

export default config;
